# 料理管理系統

純前端料理、食材與套餐規劃工具，可用 JSON 匯入匯出，適合跨裝置手動同步。

## 使用方式

- 直接開啟 `index.html` 可在瀏覽器使用。
- 部署到 GitHub Pages 後，可用 iPad Safari 開啟網址並加入主畫面。
- 離線傳檔時可使用 `standalone.html`，這是包含 CSS 與 JavaScript 的單檔版本。

## 資料同步

資料儲存在目前瀏覽器的 localStorage。跨裝置使用時，請在 JSON 分頁匯出資料，再到另一台裝置匯入。
